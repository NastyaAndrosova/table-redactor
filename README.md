Проект для редактирования табличных данных. 
Дает возможность редактировать данные, в том числе удалять, изменять таблицу (менять количество строк).
Загружать данные в формате Json и выгружать.

Для запуска проекта:
### `npm install`
### `npm start`
